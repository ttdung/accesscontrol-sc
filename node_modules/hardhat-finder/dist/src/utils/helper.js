"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.merge = exports.formatOutputName = void 0;
const deepmerge_1 = __importDefault(require("deepmerge"));
const lodash_1 = require("lodash");
const formatOutputName = (str, separator = "-") => {
    const words = str.split(separator);
    const uppercaseWords = words.map((word) => uppercaseFirstChar(word));
    const humanReadableFormat = uppercaseWords.join(" ");
    const searchRegexp = new RegExp(" ", "g");
    const pascalCaseFormat = humanReadableFormat.replace(searchRegexp, "");
    return {
        pascalCaseFormat,
        humanReadableFormat,
    };
};
exports.formatOutputName = formatOutputName;
const merge = (x, y, overrides = {}, options) => {
    let result;
    if (y !== undefined) {
        if (options?.clone) {
            const clone = (0, lodash_1.cloneDeep)(y);
            result = (0, deepmerge_1.default)(x, clone);
        }
        else {
            result = (0, deepmerge_1.default)(x, y);
        }
    }
    else {
        result = x;
    }
    result = (0, deepmerge_1.default)(result, overrides);
    return result;
};
exports.merge = merge;
const uppercaseFirstChar = (str) => {
    return str.charAt(0).toUpperCase() + str.slice(1);
};
//# sourceMappingURL=helper.js.map